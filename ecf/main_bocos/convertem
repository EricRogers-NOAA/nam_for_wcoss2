#
set files = ( `cat list` )
foreach file ($files)
sed 's/cyc=12/cyc=18/g' $file > $file.new
mv $file.new $file
end

set files = ( `cat list` )
foreach file ($files)
sed 's/_12/_18/g' $file > $file.new
mv $file.new $file
end
